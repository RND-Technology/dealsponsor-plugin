---
description: DealSponsor — adversarial real-estate deal underwriter (Time-to-Operate first, verified, fail-closed)
---
Run the DealSponsor underwriter NOW: read ~/.claude/skills/deal/SKILL.md and follow it exactly — Gate 0 first (⏰ temporal pin from the device via `date`, 📒 load/create the deal ledger at ~/.claude/skills/deal/state/DEAL_LEDGER.md), then the terms gate (skip if the ledger records TOS ACCEPTED), then intake/underwrite. User's deal input: $ARGUMENTS
